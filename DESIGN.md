# التصميم «الفولاذ» — الدليل

> الواجهة الآن لها **تصميم واحد** اسمه **«الفولاذ» (Çelik)**.
> التصميم القديم أُزيل من النظام بالكامل — لا زرّ تبديل، ولا رابط، ولا أي أثر منه
> على الشاشة. نسخته محفوظة في الأرشيف داخل المستودع، خارج مجلّد الموقع، فلا
> يُحمَّل أبدًا ولا يراه أحد.

---

## 1) أين ذهب التصميم القديم؟

الملف نفسه ما زال موجودًا — لم يُحذف — لكنه **خرج من مجلّد `public/`**:

```
archive/theme-classic.css
```

مجلّد `public/` هو وحده ما يقدّمه الخادم للمتصفّح (`server.js` سطر ٥٣).
أي ملف خارجه غير قابل للفتح من الإنترنت إطلاقًا، حتى لو عرف أحدهم اسمه.

وأُزيل معه كل ما كان يستدعيه:

| ما كان | ماذا صار |
|---|---|
| زرّ «الفولاذ / الكلاسيكي» في الشريط العلوي | حُذف نهائيًا |
| `public/theme.js` (يختار المظهر) | حُذف |
| `?theme=classic` في الرابط | لم يعد له أي معنى |
| `data-theme` في وسم `<html>` | أُزيل — لا يوجد مظهر يُختار |
| أربع عبارات «التصميم/الفولاذ/الكلاسيكي/تبديل» في ملفات اللغات | حُذفت |
| `theme-classic.css` و`theme.js` في ذاكرة الـ PWA | أُزيلا من قائمة التخزين المسبق |

### إن أردت يومًا استرجاعه

لا شيء ضاع. أمامك طريقان:

```bash
git checkout cf5b8b0
```

يُرجع كل الملفات إلى ما كانت عليه قبل التصميم الجديد كله. فرع `migrate/sqlite2`
و`main` لم يُلمسا إطلاقًا.

أو، لاسترجاع المظهر القديم فقط دون التراجع عن باقي العمل: انسخ
`archive/theme-classic.css` إلى `public/`، ووجّه وسم `<link>` في الصفحات إليه.

---

## 2) ما الذي تغيّر عن التصميم القديم

**لم يتغيّر:** ترتيب أي عنصر، ولا أي حقل، ولا أي زر، ولا أي منطق أو بيانات.
كل مسارات الـ API ووظائف النظام كما هي حرفيًا.

**تغيّر المظهر فقط:**

| العنصر | قبل | بعد |
|---|---|---|
| الزوايا | دائرية 8–16 بكسل | شبه قائمة 2 بكسل |
| الظلال | ظلال ناعمة | خطوط شعرة بدل الظلال |
| الرأس | أبيض | رمادي فولاذي داكن + شريط كهرماني |
| اللون الأساسي | بنفسجي متدرّج `#4f46e5→#7c3aed` | رمادي فولاذي `#2b3a4a` |
| لون التنبيه | برتقالي | كهرماني تحذيري `#e8a33d` |
| الخط | Inter | IBM Plex Sans + أرقام IBM Plex Mono |
| حجم الخط | ١٣ بكسل في الجداول | ١٤ بكسل |
| عرض الصفحة | ١٢٠٠ بكسل | ١٤٤٠ بكسل |
| أسماء الخانات والأعمدة | وزن ٥٠٠ | **غامقة (وزن ٧٠٠)** |
| العناوين الصغيرة | عادية | حروف كبيرة متباعدة |
| تبويبا «التقارير النشطة / الأرشيف» | نص بلا إطار | زرّان حقيقيان: المُفعّل مملوء بخطّ كهرماني سفلي، والآخر بإطار |
| الأيقونات | إيموجي (٣٧٠ موضعًا) | أيقونات SVG موحّدة مرسومة بخط واحد |
| الجداول | صفوف بيضاء | رأس داكن + صفوف متناوبة |

**أثر جانبي مفيد:** الصفحات صارت أقصر — صف الجدول ١١٢ بكسل بدل ١٣٩ (أقصر ٢٠٪)
بنفس المحتوى ونفس قابلية القراءة.

---

## 3) الملفات

| الملف | الدور |
|---|---|
| `public/styles.css` | التصميم كله — الألوان والمقاسات وتخطيط الهاتف |
| `public/icons.js` | يستبدل الإيموجي بأيقونات SVG أثناء التشغيل |
| `public/mobile-form.js` | شريط التقدّم وطيّ الأقسام في نموذج المُحمِّل (الهاتف فقط) |
| `public/sw-update.js` | يُنزل التحديث على الشاشة من أوّل فتحة، دون المساس بعمل جارٍ |
| `archive/theme-classic.css` | التصميم القديم، محفوظ خارج الموقع ولا يُقدَّم أبدًا |

---

## 4) تغيير لون النظام كله من مكان واحد

كل الألوان مُعرَّفة في أعلى `public/styles.css` داخل `:root`.
مثلًا لتغيير لون التحذير الكهرماني إلى أزرق، غيّر سطرًا واحدًا:

```css
--accent: #e8a33d;   /*  ←  غيّر هذا فقط  */
```

وينعكس على الشريط العلوي، والشعار، وتبويب الصفحة النشط، وحدود النوافذ.

**حجم الخط كله** يُتحكَّم به من كتلة `--fs-*` في نفس المكان. مثلًا لتكبير كل شيء
أكثر، ارفع القيم:

```css
--fs-13: 14px;   /*  ←  اجعلها 15px ليكبر نص الجداول أكثر  */
```

**عرض الصفحة** من `.header-content, .main-content { max-width: 1440px }`.

> ⚠️ بعد أي تعديل على `styles.css` أو `icons.js` أو `mobile-form.js`، ارفع رقم
> النسخة في `public/sw.js` (`yukleme-cache-vNN`) — وإلا بقيت المتصفّحات تعرض
> النسخة القديمة من الذاكرة المؤقّتة.

---

## 5) نسخة الهاتف

الهاتف ليس نسخة مصغّرة من الحاسوب — له تخطيط مصمَّم له وحده، يبدأ عند عرض ٧٦٨ بكسل فأقل.

### ما الذي كان معطوبًا (قياسات حقيقية على ٣٧٥×٨١٢)

| | قبل | بعد |
|---|---|---|
| قبل أول تقرير في لوحة المدير | ١٣٧٩ بكسل | **٥٤٣** |
| الشريط العلوي | ١٧٦ (ثلاثة صفوف) | **١٠٢** (صفّان، وزرّ الخروج مكتوب) |
| الإحصاءات | ٢٨٦ (بطاقات فوق بعضها) | **٥٧** (شريط ثلاثي) |
| الفلاتر | ٤٠٢ مفتوحة دائمًا | مطويّة خلف زرّ ٤٦ بكسل |
| شريط الأشهر | ١٦٩ (ثلاثة أسطر) | **٩٧** (العنوان بسطر، والأشهر بسطر يُمرَّر ويبدأ عند الشهر الحالي) |
| صفّ التقرير | جدول ٩٦٧ بكسل — **٦٣٤ مخفيّة** | **بطاقة، كل الحقول ظاهرة، صفر تمرير أفقي** |
| زرّ «تسجيل القيد» | على عمق ٢٣١٦ بكسل | مثبَّت أسفل الشاشة |

### أهمّ تغيير: الجدول يصير بطاقات

ثمانية أعمدة لا تتّسع في ٣٣٣ بكسل مهما صغّرنا الخط. كان المدير مضطرًّا للتمرير الأفقي
في **كل صفّ** ليرى عمودَي «مسجّل/بانتظار» وزرّ التفاصيل — وهي جوهر عمله.

الآن كل تقرير بطاقة واحدة تعرض: العميل والوجهة والتاريخ واللوحة ومَن أدخله ومتى،
ثم حالتَي التسجيل، ثم زرّ التفاصيل بعرض البطاقة كاملة. البطاقة ١٧٤ بكسل.

**كل حالة مكتوب بجانبها اسم صاحبها (Fayık / Pinar).** في الحاسوب يعرفهما المستخدم من
رأس الجدول، أمّا في الهاتف فالرأس مخفيّ — فكانت شارتان بلا هويّة. الاسم صار ملازمًا
للشارة، وزرّ التفاصيل نزل إلى سطر خاصّ به لأن الثلاثة كانوا يتراكبون فوق بعضهم.

**تحت ٤٠٠ بكسل** تُستَرَدّ هوامش البطاقة لتتّسع الأسطر، وتحت ٣٦٠ ينزل ما لا يتّسع
إلى سطر ثانٍ بدل أن يُقصّ. مُقاس على ٣٢٠ و٣٦٠ و٣٧٥ و٣٩٠ و٤٣٠ بكسل: لا تراكب، ولا
نصّ مقصوص، ولا شيء يخرج خارج البطاقة.

> **مقايضة صريحة:** الصفحة صارت أطول (١٦٣٨٨ بدل ١٠١٦٧ بكسل) — لأنها تعرض كل شيء
> بدل إخفاء ثلثيه خلف تمرير أفقي. الوصول لتقرير معيّن يكون بالبحث أو بشريط الأشهر،
> لا بالتمرير عبر ٨٦ بطاقة.

### نموذج المُحمِّل

- **شريط تقدّم** ثابت أعلى الشاشة: «القسم ٣ من ٧ · معلومات الوجهة» وعدد الحقول المكتملة.
- **الأقسام المكتملة تنطوي** بعلامة ✓ حين تنتقل لقسم آخر (٦٣١ ← ٦٧ بكسل للقسم الواحد).
  اضغط على عنوان أي قسم لفتحه أو طيّه يدويًا.
- **زرّ الإرسال مثبَّت** في الأسفل بدل أن يكون على بُعد ٣١٩٢ بكسل.

هذا كلّه في `public/mobile-form.js` — لا يعمل إطلاقًا على الحاسوب.
وإن أدرتَ الهاتف أفقيًا وتجاوز العرض ٧٦٨ بكسل، يختفي الشريط ويعود التخطيط العادي،
ثم يرجع فور إعادة الهاتف عموديًا.

### لضبط عتبة الهاتف

كل ما سبق داخل `@media (max-width: 768px)` في `public/styles.css`.

---

## 6) ملاحظات جودة تمّ التحقّق منها

- **التباين:** كل النصوص تتجاوز معيار AA للوصولية (٤٫٥:١ للنص الصغير).
  للمقارنة، التصميم القديم كان فيه نصوص عند **٢٫٣٤:١** (نص البحث الشبح)
  و**٢٫٨٦:١** (شارة «بانتظار») — أي غير مقروءة فعليًا.
- **الهاتف:** كل أزرار وحقول الإدخال لا تقلّ عن **٤٤ بكسل** ارتفاعًا (مهم لمسؤول التحميل
  في الساحة). لا يوجد أي تجاوز أفقي للشاشة عند عرض ٣٧٥ بكسل.
- **الطباعة:** أوراق التقارير تُطبع بالأبيض والأسود بخطوط واضحة، والأيقونات تُطبع
  كخطوط سوداء.
- **صفحة «لا يوجد اتصال»:** لها نسخة مستقلّة من الألوان داخلها (لأنها تعمل بلا شبكة).
  صارت تحمل ألوان الفولاذ فقط — لا نسخة ثانية من الألوان القديمة.
- **الترجمة:** كان كل نصّ تبنيه الجافاسكربت (شارات الحالة، زرّ التفاصيل) يبقى
  بالتركية حتى يبدّل المستخدم اللغة بيده، لأن ملف اللغات كان يُعلن عن نفسه عند
  **التبديل** فقط لا عند أوّل تحميل. صار يُعلن في الحالتين.
- **PWA — التحديث يصل من أوّل فتحة:** كان يحتاج فتحتين، لأن صفحة تُفتح أثناء تثبيت
  النسخة الجديدة تأخذ HTML من الشبكة لكن التنسيق من مخزن النسخة السابقة.
  `public/sw-update.js` يستمع للحظة تسلّم العامل الجديد ويُعيد التحميل مرّة واحدة —
  **لكن فقط إن لم يكن هناك ما يضيع**: لا حقل ملأه المستخدم، ولا نافذة مفتوحة، ولا
  إرسال جارٍ. وإن كانت الصفحة مشغولة ينتظر حتى تهدأ. رقم الكاش `v76`.

- **ألوان النسخة المثبَّتة على الهاتف:** الشريط الذي يرسمه الهاتف **فوق** التطبيق
  (شريط الحالة) كان ما يزال بنفسجي التصميم القديم `#4f46e5` — في `manifest.json`
  وفي وسم `theme-color` في الصفحات الأربع وفي `msapplication-TileColor`.
  صار `#1a242f` وهو لون شريط الرأس نفسه (`--chrome-bg`)، فيتّصل الشريطان بلا فاصل.
  وخلفية شاشة البدء صارت `#eef0f2` بدل الأبيض.

  > **مهم في النسخة المثبَّتة:** المتصفّح لا يبحث عن تحديث إلا عند **فتح جديد**.
  > إن كان التطبيق ما يزال في الخلفية فالضغط على أيقونته يستأنفه فقط ولا يبحث.
  > لضمان وصول التحديث فورًا: **اسحب التطبيق من قائمة التطبيقات الأخيرة ثم افتحه.**
  > وإلا وصل من تلقاء نفسه حين يُعيد المتصفّح الفحص (خلال يوم تقريبًا).

  > للفحص عند الشكوى: اكتب `swUpdate.blockedBy()` في وحدة تحكّم المتصفّح — يقول لك
  > ما الذي يمنع التحديث (`dialog` أو `submit` أو `filled-field`) أو `null` إن كان جاهزًا.

---

## 7) إن أردت تعديل شيء

- **لون أو مقاس عام** ← `public/styles.css` في كتلة `:root` بالأعلى.
- **شكل أيقونة** ← `public/icons.js` في جدول `ICONS`.
- **أي إيموجي جديد يظهر مستقبلًا** ← أضِف سطرًا في جدول `EMOJI` داخل `icons.js`.
