# التصميم الجديد «الفولاذ» — وكيفية التراجع

> باختصار: الواجهة الآن لها **مظهران**. الجديد اسمه **«الفولاذ» (Çelik)** وهو المُفعّل افتراضيًا،
> والقديم محفوظ كما هو تمامًا باسم **«الكلاسيكي» (Klasik)**. التبديل بينهما بضغطة زر،
> ولا يحتاج أي إعادة نشر ولا يمسّ أي بيانات.

---

## 1) كيف أرجع للتصميم القديم؟ (ثلاث طرق، من الأسهل للأقوى)

### الطريقة ١ — زر في أعلى الصفحة (الأسرع)
في شريط الرأس، بجانب مبدّل اللغة، يوجد زر مكتوب عليه **ÇELİK**.
اضغطه ← تتحوّل الواجهة فورًا إلى الشكل القديم ويصير مكتوبًا عليه **KLASİK**.
اضغطه مرة أخرى للعودة للجديد.

الاختيار **يُحفظ في المتصفّح**، فيبقى كما تركته في كل مرة تفتح فيها النظام.
كل مستخدم يختار لنفسه — اختيارك لا يؤثّر على بقية المستخدمين.

### الطريقة ٢ — من الرابط
أضف `?theme=classic` إلى أي رابط:

```
https://yukleme.karahoca.com/manager.html?theme=classic
```

يعمل حتى لو لم تجد الزر. وللعودة: `?theme=steel`.

### الطريقة ٣ — التراجع الكامل عن الكود (إن أردت إلغاء التصميم الجديد نهائيًا)

```bash
git checkout migrate/sqlite2
```

هذا يُرجع كل الملفات إلى ما كانت عليه قبل التصميم الجديد. كل العمل الجديد على فرع
`design/steel` منفصل تمامًا، ولم يُلمس فرع `migrate/sqlite2` ولا `main`.

---

## 2) ما الذي تغيّر بالضبط

**لم يتغيّر:** ترتيب أي عنصر، ولا أي حقل، ولا أي زر، ولا أي منطق أو بيانات.
كل مسارات الـ API ووظائف النظام كما هي حرفيًا.

**تغيّر المظهر فقط:**

| العنصر | قبل | بعد |
|---|---|---|
| الزوايا | دائرية 8–16 بكسل | شبه قائمة 2 بكسل |
| الظلال | ظلال ناعمة | خطوط شعرة بدل الظلال |
| الرأس | أبيض | رمادي فولاذي داكن + شريط كهرماني |
| اللون الأساسي | بنفسجي متدرّج `#4f46e5→#7c3aed` | رمادي فولاذي `#2b3a4a` |
| لون التنبيه | برتقالي | كهرماني تحذيري `#e8a33d` |
| الخط | Inter | IBM Plex Sans + أرقام IBM Plex Mono |
| حجم الخط | صغير (١٣ بكسل في الجداول) | أكبر ~١٥٪ (١٥ بكسل في الجداول) |
| عرض الصفحة | ١٢٠٠ بكسل | ١٦٨٠ بكسل |
| أسماء الخانات والأعمدة | وزن ٥٠٠ | **غامقة (وزن ٧٠٠)** |
| العناوين الصغيرة | عادية | حروف كبيرة متباعدة |
| تبويبا «التقارير النشطة / الأرشيف» | نص بلا إطار | زرّان حقيقيان: المُفعّل مملوء بخطّ كهرماني سفلي، والآخر بإطار |
| الأيقونات | إيموجي (٣٧٠ موضعًا) | أيقونات SVG موحّدة مرسومة بخط واحد |
| الجداول | صفوف بيضاء | رأس داكن + صفوف متناوبة |

**أثر جانبي مفيد:** الصفحات صارت أقصر — صف الجدول ١١٢ بكسل بدل ١٣٩ (أقصر ٢٠٪)
بنفس المحتوى ونفس قابلية القراءة.

---

## 3) الملفات

| الملف | الدور |
|---|---|
| `public/styles.css` | التصميم الجديد «الفولاذ» |
| `public/theme-classic.css` | التصميم القديم كما هو (لا تُعدّله) |
| `public/theme.js` | يختار المظهر قبل أول رسم للصفحة + يركّب زر التبديل |
| `public/icons.js` | يستبدل الإيموجي بأيقونات SVG (لا يعمل إطلاقًا في الوضع الكلاسيكي) |

### لماذا الرجوع مضمون ومطابق ١٠٠٪
- التصميم القديم لم يُعدَّل، بل نُسخ كما هو إلى `theme-classic.css`. الاستثناء
  الوحيد: كتلة صغيرة مُضافة في آخر الملف تُعيد شكل زرَّي «التقارير النشطة /
  الأرشيف» — كان شكلهما مكتوبًا داخل وسم HTML مباشرةً، فنُقل إلى CSS ليتمكّن كل
  تصميم من تنسيقهما؛ والقيم في تلك الكتلة هي القيم الأصلية نفسها حرفيًا.
- الألوان والزوايا المكتوبة داخل صفحات HTML صارت مكتوبة هكذا:
  `var(--brand, #4f46e5)` — أي «استخدم لون النظام الجديد، وإن لم يكن موجودًا فاستخدم القيمة القديمة».
  الوضع الكلاسيكي لا يعرّف تلك المتغيّرات ⟵ فتعود القيم القديمة تلقائيًا.
- `icons.js` يتوقّف عن العمل تمامًا في الوضع الكلاسيكي ⟵ تعود كل الإيموجي كما كانت.

**مُتحقَّق منه عمليًا:** بعد التبديل إلى الكلاسيكي رجعت ٩٥٤ إيموجي، وخط Inter،
والزوايا ١٦ بكسل، والتدرّج البنفسجي — أي التصميم القديم بالضبط.

---

## 4) تغيير لون النظام كله من مكان واحد

كل الألوان مُعرَّفة في أعلى `public/styles.css` داخل `:root`.
مثلًا لتغيير لون التحذير الكهرماني إلى أزرق، غيّر سطرًا واحدًا:

```css
--accent: #e8a33d;   /*  ←  غيّر هذا فقط  */
```

وينعكس على الشريط العلوي، والشعار، وتبويب الصفحة النشط، وحدود النوافذ.

**حجم الخط كله** يُتحكَّم به من كتلة `--fs-*` في نفس المكان. مثلًا لتكبير كل شيء
أكثر، ارفع القيم:

```css
--fs-13: 15px;   /*  ←  اجعلها 16px ليكبر نص الجداول أكثر  */
```

**عرض الصفحة** من `.header-content, .main-content { max-width: 1680px }`.

> ⚠️ بعد أي تعديل على `styles.css` أو `icons.js`، ارفع رقم النسخة في
> `public/sw.js` (`yukleme-cache-vNN`) — وإلا بقيت المتصفّحات تعرض النسخة
> القديمة من الذاكرة المؤقّتة.

---

---

## 4.5) نسخة الهاتف

الهاتف ليس نسخة مصغّرة من الحاسوب — له تخطيط مصمَّم له وحده، يبدأ عند عرض ٧٦٨ بكسل فأقل.
كل ما يلي في التصميم الجديد فقط؛ الوضع الكلاسيكي يبقى كما كان تمامًا على الهاتف أيضًا.

### ما الذي كان معطوبًا (قياسات حقيقية على ٣٧٥×٨١٢)

| | قبل | بعد |
|---|---|---|
| قبل أول تقرير في لوحة المدير | ١٣٧٩ بكسل | **٥٤٣** |
| الشريط العلوي | ١٧٦ (ثلاثة صفوف) | **١٠٥** (صفّان) |
| الإحصاءات | ٢٨٦ (بطاقات فوق بعضها) | **٥٧** (شريط ثلاثي) |
| الفلاتر | ٤٠٢ مفتوحة دائمًا | مطويّة خلف زرّ ٤٦ بكسل |
| شريط الأشهر | ١٦٩ (ثلاثة أسطر) | **٥٩** (سطر واحد يُمرَّر) |
| صفّ التقرير | جدول ٩٦٧ بكسل — **٦٣٤ مخفيّة** | **بطاقة، كل الحقول ظاهرة، صفر تمرير أفقي** |
| زرّ «تسجيل القيد» | على عمق ٢٣١٦ بكسل | مثبَّت أسفل الشاشة |

### أهمّ تغيير: الجدول يصير بطاقات

ثمانية أعمدة لا تتّسع في ٣٣٣ بكسل مهما صغّرنا الخط. كان المدير مضطرًّا للتمرير الأفقي
في **كل صفّ** ليرى عمودَي «مسجّل/بانتظار» وزرّ التفاصيل — وهي جوهر عمله.

الآن كل تقرير بطاقة واحدة تعرض: العميل والوجهة والتاريخ واللوحة ومَن أدخله ومتى،
ثم الحالتان وزرّ التفاصيل. البطاقة ١٥١ بكسل.

> **مقايضة صريحة:** الصفحة صارت أطول (١٤٣٦٩ بدل ١٠١٦٧ بكسل) — لأنها تعرض كل شيء
> بدل إخفاء ثلثيه خلف تمرير أفقي. الوصول لتقرير معيّن يكون بالبحث أو بشريط الأشهر،
> لا بالتمرير عبر ٨٦ بطاقة.

### نموذج المُحمِّل

- **شريط تقدّم** ثابت أعلى الشاشة: «القسم ٣ من ٧ · معلومات الوجهة» وعدد الحقول المكتملة.
- **الأقسام المكتملة تنطوي** بعلامة ✓ حين تنتقل لقسم آخر (٦٣١ ← ٦٧ بكسل للقسم الواحد).
  اضغط على عنوان أي قسم لفتحه أو طيّه يدويًا.
- **زرّ الإرسال مثبَّت** في الأسفل بدل أن يكون على بُعد ٣١٩٢ بكسل.

هذا كلّه في `public/mobile-form.js` — لا يعمل إطلاقًا على الحاسوب ولا في الوضع الكلاسيكي.

### لضبط عتبة الهاتف

كل ما سبق داخل `@media (max-width: 768px)` في `public/styles.css`.

---

## 5) ملاحظات جودة تمّ التحقّق منها

- **التباين:** كل النصوص تتجاوز معيار AA للوصولية (٤٫٥:١ للنص الصغير).
  للمقارنة، التصميم القديم كان فيه نصوص عند **٢٫٣٤:١** (نص البحث الشبح)
  و**٢٫٨٦:١** (شارة «بانتظار») — أي غير مقروءة فعليًا.
- **الهاتف:** كل أزرار وحقول الإدخال لا تقلّ عن **٤٤ بكسل** ارتفاعًا (مهم لمسؤول التحميل
  في الساحة). لا يوجد أي تجاوز أفقي للشاشة عند عرض ٣٧٥ بكسل.
- **الطباعة:** أوراق التقارير تُطبع بالأبيض والأسود بخطوط واضحة، والأيقونات تُطبع
  كخطوط سوداء.
- **PWA:** رُفعت نسخة الكاش إلى `v72` وأُضيفت الملفات الجديدة للتخزين المسبق،
  فيصل التحديث لكل الأجهزة تلقائيًا.

---

## 6) إن أردت تعديل شيء

- **لون أو مقاس عام** ← `public/styles.css` في كتلة `:root` بالأعلى.
- **شكل أيقونة** ← `public/icons.js` في جدول `ICONS`.
- **أي إيموجي جديد يظهر مستقبلًا** ← أضِف سطرًا في جدول `EMOJI` داخل `icons.js`.
