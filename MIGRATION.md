# دليل النقل الكامل: من Render + Supabase إلى Hetzner + Coolify + SQLite

> هذا الدليل مُفصّل عمدًا خطوة بخطوة. اتبعه بالترتيب. لا شيء فيه يحذف أو يغيّر بياناتك على
> Supabase — العملية **نسخ** فقط، و**Render يبقى يعمل** طوال الوقت كشبكة أمان.

---

## 0) المبدأ: لماذا لن نفقد بيانات

| الضمان | التفصيل |
|--------|---------|
| قراءة فقط من المصدر | سكربت النقل يقرأ من Supabase فقط، لا يحذف ولا يعدّل. تبقى نسختك هناك سليمة 100%. |
| Render يبقى حيًّا | لا توقفه إلا في الخطوة الأخيرة بعد أن يعمل كل شيء على الخادم الجديد. |
| تحقّق بالأرقام | السكربت يقارن عدد الصفوف في Supabase مقابل ما صُدِّر مقابل ما أُدرِج في SQLite، ويطبع تقريرًا. |
| قابلية التراجع الكاملة | كل العمل على فرع `migrate/sqlite`. الأمر `git checkout main` يرجّع كل شيء كما كان. |
| تجربة قبل الاعتماد | تختبر محليًا على جهازك أولًا، ثم على الخادم، قبل تحويل الدومين. |

---

## 1) ما الذي تغيّر بالضبط

### ملفات جديدة
- `lib/sqlite-schema.sql` — مخطط قاعدة SQLite (مطابق 1:1 للمخطط الحيّ على Supabase).
- `lib/supabase-sqlite.js` — **المحوّل**: كائن متوافق مع واجهة Supabase لكنه يقرأ/يكتب على SQLite. بفضله لم يتغيّر منطق `server.js` تقريبًا.
- `migrate/migrate.js` — سكربت النقل لمرة واحدة (تصدير + تنزيل صور + تحقق).
- `Dockerfile` و `.dockerignore` — لبناء صورة الإنتاج على Coolify.
- `.env.example` — قائمة كل متغيّرات البيئة موثّقة.
- `MIGRATION.md` — هذا الملف.

### ملفات معدّلة
- `database.js` — صار يفتح SQLite بدل Supabase (نفس الواجهة `{ supabase, initializeDatabase }`).
- `server.js` — التخزين صار محليًا (`/uploads`) بدل Supabase Storage، وأُضيف مسار `/healthz`.
- `utils/telegramBot.js` — رابط "عرض التقرير" صار من متغيّر `PUBLIC_URL`.
- `public/sw.js` — رُفِعت نسخة الكاش إلى `v37`، واستُثني `/uploads/` من الكاش.
- `package.json` — أُضيف `better-sqlite3` وأوامر `migrate`.
- سكربتات `scripts/*` و `mark_papers.js` — تستخدم محوّل SQLite بدل عميل Supabase.
- `.gitignore` — يحمي ملفات قاعدة البيانات والصور المحلية.

### نسخة احتياطية للتراجع
- `database.supabase.js.bak` — نسخة من `database.js` الأصلي (Supabase). محفوظة محليًا (مُستثناة من git).

### ما الذي **لم** يتغيّر
- كل مسارات الـ API (`/api/login`, `/api/loadings` ...) كما هي.
- واجهة المستخدم (`loader.html`, `manager.html`) — لا تتصل بـ Supabase أصلًا، فلا تحتاج تعديلًا.
- منطق الأدوار، النسخ، التقييد، الأوراق، طلبات التحسين — نفسه تمامًا.

---

## 2) كيفية التراجع (Rollback) في أي لحظة

**تراجع كامل عن كل التغييرات:**
```bash
git checkout main
npm install        # يعيد الاعتماديات الأصلية
```
هذا يرجّعك إلى نسخة Supabase الأصلية بالضبط. بياناتك على Supabase لم تُمسّ.

**تراجع جزئي (إبقاء الفرع لكن إرجاع ملف واحد):**
```bash
git checkout main -- server.js     # مثال: إرجاع ملف واحد
```

**على الخادم:** ما دام Render شغّالًا، يمكنك ببساطة إرجاع الدومين إليه (الخطوة في القسم 9).

---

## 3) الجزء أ — اختبار محلي على جهازك (قاعدة فارغة)

الهدف: التأكد أن التطبيق يعمل على SQLite قبل لمس أي بيانات حقيقية.

1. ثبّت الاعتماديات (مرة واحدة):
   ```bash
   npm install
   ```
2. شغّل التطبيق:
   ```bash
   npm start
   ```
   سيُنشئ تلقائيًا ملف `./data/app.db` ويزرع 4 مستخدمين افتراضيين.
3. افتح المتصفح على `http://localhost:5000`.
4. سجّل الدخول بأحد الحسابات الافتراضية للاختبار:
   - loader: `murat` / `murat123`
   - manager: `manager` / `manager123`
5. جرّب: إنشاء تقرير مع صورة، عرضه كمدير، تعديله، تسجيل القيد. لاحظ أن الصور تُحفظ في مجلّد `./uploads`.

> ✅ إذا عمل كل ذلك، فالكود سليم. الآن ننتقل لنقل بياناتك الحقيقية.

> 💡 لإعادة التجربة من الصفر: احذف مجلّد `data/` (يحذف القاعدة المحلية) وأعد التشغيل.

---

## 4) الجزء ب — ⚠️ ما يجب أن تفعله **أنت بنفسك** على Supabase

السكربت يحتاج مفتاحًا واحدًا فقط من لوحة Supabase. هذا **العمل الوحيد** المطلوب منك يدويًا.

### جلب مفتاح `service_role` (موصى به بشدّة)
1. ادخل إلى **https://supabase.com** وافتح مشروعك.
2. من القائمة السفلية اليسرى: **⚙️ Project Settings**.
3. اضغط **API**.
4. انزل إلى قسم **Project API keys**.
5. ستجد مفتاحين: `anon` `public` و `service_role` `secret`. انسخ مفتاح **`service_role`** (اضغط "Reveal" ثم انسخ).
6. انسخ أيضًا **Project URL** (في الأعلى، مثل `https://abcd1234.supabase.co`).

> 🔒 مفتاح `service_role` سرّي جدًا (يتجاوز كل صلاحيات الأمان). لا تضعه في git، ولا ترسله لأحد، واحذفه من `.env` بعد انتهاء النقل.

> لماذا `service_role` وليس `anon`؟ لأن جدول النسخ القديمة `loading_versions` غير مقروء بمفتاح `anon` (لا توجد عليه سياسة قراءة). مفتاح `service_role` يقرأ **كل شيء** بلا عوائق، فلا تحتاج لتغيير أي إعداد في Supabase.

> إن تعذّر عليك إيجاد مفتاح `service_role` واضطُررت لاستخدام `anon`: عندها فقط نفّذ هذا في Supabase ← **SQL Editor**:
> ```sql
> CREATE POLICY "public read versions" ON loading_versions FOR SELECT USING (true);
> ```
> ثم استخدم مفتاح `anon`. (مع `service_role` لا تحتاج هذا إطلاقًا.)

---

## 5) الجزء ج — تشغيل سكربت النقل والتحقق (على جهازك أولًا)

1. أنشئ ملف `.env` (انسخ من `.env.example`) وضع فيه على الأقل:
   ```
   SUPABASE_URL=https://<your-project>.supabase.co
   SUPABASE_SERVICE_KEY=<service_role secret key>
   DB_PATH=./data/app.db
   UPLOADS_DIR=./uploads
   ```
2. **جرّب أولًا بدون كتابة شيء** (تصدير + عدّ فقط):
   ```bash
   npm run migrate:dry
   ```
   راقب المخرجات: يجب أن يطابق عدد المُصدَّر عدد Supabase لكل جدول.
3. إذا كانت الأرقام منطقية، شغّل النقل الفعلي:
   ```bash
   npm run migrate
   ```
   هذا: يصدّر الصفوف، ينزّل كل الصور إلى `./uploads`، يعيد كتابة الروابط، ويملأ `./data/app.db`.
4. افتح تقرير التحقق:
   - `migrate/output/report.json` — الأعداد (remote / exported / inserted) وعدد الملفات.
   - `migrate/output/errors.log` — أي تحذيرات (يُنشأ فقط عند وجود مشاكل).
   - يجب أن ترى في النهاية: `✅ Migration completed with NO errors and all counts match.`
5. **اختبر بياناتك الحقيقية محليًا**: شغّل `npm start`، وافتح `http://localhost:5000`، وسجّل الدخول **ببيانات حساباتك الحقيقية** (نفس أسماء المستخدمين/كلمات المرور التي على Render). تأكد أن التقارير والصور والنسخ تظهر صحيحة.

> 💡 السكربت قابل لإعادة التشغيل: يمسح جداول SQLite الهدف ثم يعيد التحميل. لا خطر من تكراره.

---

## 6) الجزء د — النشر على Hetzner عبر Coolify (خطوة بخطوة)

### 6.1 دفع الكود إلى GitHub
الفرع الحالي `migrate/sqlite`. ادفعه:
```bash
git add -A
git commit -m "Migrate from Supabase to SQLite + local storage; add Coolify Dockerfile"
git push -u origin migrate/sqlite
```
> يمكنك لاحقًا دمجه في `main` بعد نجاح كل شيء. Coolify يستطيع النشر من أي فرع تحدّده.

### 6.2 إنشاء التطبيق في Coolify
1. في Coolify: **Project** جديد (أو موجود) ← **Environment** (مثلًا production) ← **+ New** ← **Application**.
2. المصدر: **Public/Private Repository** ← الصق رابط مستودعك `https://github.com/yahyaabohashemstu/yukleme.git` (اربط حساب GitHub إن كان خاصًّا).
3. **Branch**: `migrate/sqlite` (أو `main` بعد الدمج).
4. **Build Pack**: اختر **Dockerfile** (مهم جدًا — لا تختر Nixpacks). Coolify سيستخدم `Dockerfile` الموجود في الجذر.

### 6.3 المنفذ والنطاق الفرعي `yukleme.karahoca.com`
5. **Ports Exposes**: `5000`.
6. **Domains**: ضع `https://yukleme.karahoca.com`. فعّل **HTTPS** (Coolify يصدر شهادة Let's Encrypt تلقائيًا عبر Traefik).
   - في إعدادات DNS لنطاق `karahoca.com`: أضف سجلًّا للنطاق الفرعي `yukleme`:
     - **A** record: `yukleme` → **IP خادم Hetzner**، أو **CNAME**: `yukleme` → نفس وجهة `karahoca.com`.
   - تأكد أن المنفذين 80 و443 مفتوحان على الخادم.
   - **التعايش مع مشروع React:** بما أن `karahoca.com` (مشروع React) و`yukleme.karahoca.com` كلاهما على نفس Coolify، فإن Traefik **يوجّه تلقائيًا حسب اسم النطاق (host)** — كل تطبيق له دومينه الخاص، بلا أي تعارض وبلا إعداد مسارات. كل منهما مستقل تمامًا (نشر/تحديث منفصل).

### 6.4 ⚠️ المجلّدات الدائمة (أهم خطوة — تمنع فقدان البيانات عند إعادة النشر)
بدون هذه الخطوة، تُمسح قاعدة البيانات والصور في كل إعادة نشر!
7. افتح تبويب **Storages** ← **+ Add**:
   - **Volume 1**: Name = `yukleme-db` ، Destination Path = `/app/data`
   - **Volume 2**: Name = `yukleme-uploads` ، Destination Path = `/app/uploads`
   - (اختياري) **Volume 3**: Name = `yukleme-backups` ، Destination Path = `/app/backups`

### 6.5 متغيّرات البيئة
8. افتح تبويب **Environment Variables** وأضف:
   | المفتاح | القيمة | ملاحظة |
   |---------|--------|--------|
   | `NODE_ENV` | `production` | يُفعّل الكوكي الآمن |
   | `PORT` | `5000` | نفس Ports Exposes |
   | `SESSION_SECRET` | (سلسلة عشوائية طويلة) | ولّدها بالأمر أدناه |
   | `DB_PATH` | `/app/data/app.db` | داخل مجلّد القاعدة الدائم |
   | `UPLOADS_DIR` | `/app/uploads` | داخل مجلّد الصور الدائم |
   | `PUBLIC_URL` | `https://yukleme.karahoca.com` | لرابط Telegram |
   | `TELEGRAM_BOT_TOKEN` | (التوكن — علّمه Secret) | **دوّره** (انظر التحذير الأمني) |
   | `TELEGRAM_CHAT_ID` | (معرّف المجموعة) | |

   لتوليد `SESSION_SECRET`:
   ```bash
   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
   ```

### 6.6 فحص الصحة (Healthcheck) والنشر
9. (اختياري لكن مستحسن) في إعدادات Healthcheck: المسار `/healthz`، المنفذ `5000`.
10. اضغط **Deploy**. راقب سجلّ البناء:
    - يجب أن يُبنى `better-sqlite3` بنجاح (بفضل أدوات البناء في الـ Dockerfile).
    - يجب أن ترى `🚀 Server running on http://localhost:5000`.
11. افتح الدومين. سيعمل التطبيق بقاعدة **فارغة** فيها المستخدمون الافتراضيون الأربعة (هذا مؤقت — سنملؤها بالبيانات الحقيقية في الخطوة التالية).

---

## 7) الجزء هـ — ملء بياناتك الحقيقية في الخادم

الطريقة الموصى بها: تشغيل سكربت النقل **داخل حاوية الخادم**، فيكتب مباشرةً في المجلّدات الدائمة.

1. في Coolify، افتح تطبيقك ← زر **Terminal** (أو **Execute Command**) للدخول إلى الحاوية.
2. شغّل (استبدل القيم بمفاتيحك):
   ```bash
   SUPABASE_URL=https://<your-project>.supabase.co \
   SUPABASE_SERVICE_KEY=<service_role secret> \
   node migrate/migrate.js
   ```
   - `DB_PATH` و `UPLOADS_DIR` مضبوطان مسبقًا على مساري المجلّدين الدائمين، فيكتب السكربت مباشرةً فيهما.
   - سيمسح المستخدمين الافتراضيين الأربعة ويستبدلهم ببياناتك الحقيقية، وينزّل كل الصور إلى `/app/uploads`.
3. تحقق من المخرجات: يجب أن ترى `✅ ... all counts match`.
4. **هيّئ المصغّرات** (اختياري لكن مستحسن لسرعة أول تصفّح): في نفس الـ Terminal شغّل `npm run thumbs` — يولّد مصغّرات webp لكل الصور. (إن لم تشغّله، تُولَّد تلقائيًا كسولًا عند أول طلب لكل صورة.)
5. حدّث صفحة التطبيق، وسجّل الدخول **ببياناتك الحقيقية**. تأكد أن كل التقارير والصور والنسخ موجودة.

> نفّذ هذه الخطوة **قبل** أن تعلن الرابط الجديد للمستخدمين، حتى لا يكتب أحد بيانات أثناء الاستيراد.

> **بديل أبسط وأأمن (موصى به لأن النقل المحلي تمّ وتُحقِّق منه فعلًا):** انسخ الملفات المُتحقَّقة من جهازك مباشرةً إلى مجلّدَي الخادم — `data/app.db` ← مجلّد `yukleme-db`، ومحتويات `uploads/` (بما فيها مجلّد `thumbs/` الجاهز) ← مجلّد `yukleme-uploads` — عبر `scp` ثم `docker cp`، أو مدير ملفات الخادم. بهذا لا تحتاج وضع مفاتيح Supabase على الخادم إطلاقًا، وتنقل **نفس البيانات والمصغّرات** التي تحقّقت منها. أوقف التطبيق لحظة النسخ ثم أعد تشغيله.

---

## 8) الجزء و — اختبار بقاء البيانات بعد إعادة النشر (مهم!)

للتأكد أن المجلّدات الدائمة مضبوطة فعلًا:
1. أنشئ تقريرًا تجريبيًا جديدًا مع صورة من الواجهة.
2. في Coolify اضغط **Redeploy** (إعادة نشر).
3. بعد اكتمالها، افتح التطبيق: يجب أن يبقى التقرير التجريبي وصورته موجودين.
   - ✅ إذا بقيا: المجلّدات الدائمة تعمل بشكل صحيح.
   - ❌ إذا اختفيا: المجلّدات غير مضبوطة — راجع القسم 6.4. **لا تكمل** قبل حلّ هذا.
4. احذف التقرير التجريبي.

---

## 9) الجزء ز — النسخ الاحتياطي المجدول

1. في Coolify: تطبيقك ← **Scheduled Tasks** ← **+ Add**.
2. **Frequency**: `0 3 * * *` (يوميًا الساعة 3 فجرًا).
3. **Command**:
   ```bash
   sqlite3 /app/data/app.db ".backup '/app/backups/app-$(date +%F).db'" && tar -czf /app/backups/uploads-$(date +%F).tar.gz -C /app/uploads .
   ```
   (يتطلب مجلّد `/app/backups` الدائم من الخطوة 6.4، وأداة `sqlite3` — وهي مضمّنة في صورة node bookworm عادةً؛ إن لم تكن، استخدم `cp /app/data/app.db ...` كحل بسيط، أو ثبّتها في الـ Dockerfile.)
4. (اختياري) نظّف النسخ القديمة: أضف أمرًا دوريًا `find /app/backups -name 'app-*.db' -mtime +14 -delete`.
5. (متقدّم) لحماية لحظية مستمرة، يمكن استخدام **Litestream** لبثّ نسخ SQLite إلى تخزين S3.

> اختبر الاستعادة مرة واحدة: انسخ نسخة احتياطية فوق `/app/data/app.db`، أعد التشغيل، وتأكد أن البيانات رجعت.

---

## 10) الجزء ح — التبديل النهائي وإيقاف Render

1. بعد أن يعمل كل شيء على الخادم الجديد ويُختبر:
   - وجّه دومينك النهائي إلى خادم Hetzner (سجل DNS).
   - أو إن كنت تستخدم دومين `yukleme.onrender.com`، أبلغ المستخدمين بالرابط الجديد.
2. أبقِ Render شغّالًا **يومًا أو يومين** كاحتياط قبل إيقافه.
3. بعد الاطمئنان: أوقف/احذف خدمة Render. (يمكنك إبقاء مشروع Supabase كأرشيف بارد بلا تكلفة، أو حذفه لاحقًا.)
4. ادمج فرع `migrate/sqlite` في `main`:
   ```bash
   git checkout main && git merge migrate/sqlite && git push origin main
   ```

---

## 10.5) ⚡ تحسينات الأداء (مفعّلة تلقائيًا — لا إجراء مطلوب)

كل ما يلي يعمل تلقائيًا عند النشر:

| التحسين | الأثر |
|---------|------|
| **مصغّرات الصور (webp)** | المعارض تُحمّل ~7.6MB بدل 406MB — **أصغر بنسبة 98%**. صورة 6.9MB تظهر كمصغّرة 12KB. الصورة الكاملة تُفتح فقط عند النقر. |
| **ضغط gzip** | للـ HTML/CSS/JS/JSON — صفحات أخفّ وأسرع. |
| **كاش immutable سنة كاملة** | للصور والمصغّرات — تُحمّل مرة واحدة ثم من ذاكرة المتصفح. |
| **`loading="lazy"`** | الصور تُحمّل عند ظهورها فقط. |
| **ضبط SQLite متقدّم** (WAL + cache + mmap) | قراءات أسرع. |
| **HTTP/2 + TLS** | يوفّرهما Traefik تلقائيًا. |

- المصغّرات تُولَّد **مرة واحدة** عند أول طلب وتُخزَّن في `uploads/thumbs/` (داخل المجلّد الدائم)، أو شغّل `npm run thumbs` لتهيئتها دفعةً واحدة.
- **اختياري (CDN):** لأقصى سرعة عالمية، ضع نطاق `karahoca.com` خلف **Cloudflare** (مجاني)؛ ترويسات الكاش الجاهزة تجعله يخزّن الصور على الحافة. لا يتطلب أي تعديل في الكود.

---

## 11) ✅ ملخّص: ما عليك أنت فعله بنفسك (مجمّعًا)

1. **[Supabase]** جلب مفتاح `service_role`/`secret` و Project URL (القسم 4). *(تمّ — استُخدم لإنجاز النقل المحلي.)*
2. **[جهازك]** تشغيل `npm run migrate` ومراجعة التقرير (القسم 5). *(تمّ ✅ — 5/330/305 + 332 صورة.)*
3. **[DNS]** إضافة سجل `yukleme` (A/CNAME) لنطاق `karahoca.com` → خادم Hetzner (القسم 6.3).
4. **[GitHub]** دفع الفرع `git push` (القسم 6.1).
5. **[Coolify]** إنشاء التطبيق: Dockerfile + النطاق `yukleme.karahoca.com` + **المجلّدان الدائمان** + متغيّرات البيئة (القسم 6).
6. **[Coolify]** توليد `SESSION_SECRET`، و**تدوير توكن Telegram + المفتاح السرّي** (القسم 12).
7. **[نقل البيانات]** نسخ `data/app.db` + `uploads/` (المُتحقَّق منهما، مع `thumbs/`) إلى المجلّدين — أو تشغيل النقل في الحاوية (القسم 7).
8. **[تحقق]** اختبار بقاء البيانات بعد إعادة النشر (القسم 8).
9. **[Coolify]** ضبط النسخ الاحتياطي المجدول (القسم 9).
10. **[DNS/Render]** التبديل النهائي وإيقاف Render بعد الاطمئنان (القسم 10).

---

## 12) 🔒 تنبيه أمني (مهم)

- ظهر توكن Telegram الحقيقي وقيم `.env` أثناء العمل. **دوّر توكن البوت** من BotFather (الأمر `/revoke` ثم استخدم التوكن الجديد) وضعه في Coolify فقط.
- لا تضع أي أسرار في git. ملف `.env` مُستثنى أصلًا.
- بعد انتهاء النقل، **احذف** `SUPABASE_URL` و `SUPABASE_SERVICE_KEY` من `.env` ومن متغيّرات Coolify (لم يعودا مطلوبين للتشغيل).

---

## 13) استكشاف الأخطاء (Troubleshooting)

| العَرَض | السبب المحتمل | الحل |
|--------|----------------|------|
| فشل بناء `better-sqlite3` على Coolify | لم يُختر Dockerfile (اختير Nixpacks) | غيّر Build Pack إلى **Dockerfile** |
| البيانات تختفي بعد إعادة النشر | المجلّدات الدائمة غير مضبوطة | راجع القسم 6.4 وتأكد من مساري `/app/data` و `/app/uploads` |
| `loading_versions` فارغ بعد النقل | استُخدم مفتاح `anon` بدل `service_role` | أعد التشغيل بمفتاح `service_role` (القسم 4) |
| الصور لا تظهر | فشل تنزيل بعض الملفات | راجع `migrate/output/errors.log`؛ الروابط الفاشلة تبقى كما هي ولا تُفقد البيانات |
| تسجيل الدخول لا يثبت (يخرج مباشرة) | `NODE_ENV` ليس `production` خلف HTTPS أو نقص `trust proxy` | تأكد `NODE_ENV=production` (الكود فيه `trust proxy` مسبقًا) |
| الحاوية تُعلَّم unhealthy | فحص الصحة يشير لمسار يتطلب تسجيل دخول | اجعل الـ Healthcheck على `/healthz` |
| المستخدمون يرون واجهة قديمة | كاش الـ PWA | رُفِعت النسخة إلى v37 تلقائيًا؛ اطلب منهم تحديث الصفحة |

---

تمّ إعداد كل ما سبق على فرع `migrate/sqlite`. لا شيء على `main` أو على Supabase أو على Render تأثّر.
